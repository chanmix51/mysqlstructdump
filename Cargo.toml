[package]
name = "mysqlstructdump"
version = "0.1.0"
authors = ["greg"]
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
sqlx = { version = "0.3.5", features = ["mysql"] }
futures = "0.3"
